node_modules/
.next/
dist/
.env
.env.local
data/
*.log
.DS_Store
next-env.d.ts
tsconfig.tsbuildinfo
.superpowers/

# Redis 在项目目录里跑时写的快照。是运行时垃圾,不该进仓库 ——
# 它可能含会话/审批负载,而且每次跑测试都变动,污染 diff。
dump.rdb
*.rdb
.idea/
# Claude Code 的个人本地设置(权限白名单等)。同目录下的 settings.json 是
# 团队共享配置,仍应入库 —— 所以只忽略 .local 这一份,不忽略整个 .claude/
.claude/settings.local.json
